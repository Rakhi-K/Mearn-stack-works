var age = 25;  //integer
console.log(typeof (age));    /// to output tyrp log(type(varialble name));


var average = 10.5;   //num
console.log(typeof (average));

var c_name = "rakhi"; //string 
console.log(typeof (c_name));

var is_active = true; //boolean
console.log(typeof (is_active));


//types-  num, string, boolean, undefined- variable is there- not initialized, null-


var num1 = 10;
var num2 = "10";

console.log(num1 === num2 ? "both are same" : "not same");  // here it compare the type & content also

var num1 = 9;
var num2 = "9";

console.log(num1 == num2 ? "both are same" : "not same");  //here ==  - operator compare only content, not type


